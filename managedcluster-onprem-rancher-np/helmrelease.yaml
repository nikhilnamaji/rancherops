apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: klusterlet
  namespace: flux-system
spec:
  interval: 1m
  releaseName: open-cluster-management-agent-klusterlet
  targetNamespace: open-cluster-management-agent
  install:
    createNamespace: true
  chart:
    spec:
      chart: klusterlet
      version: "0.15.2"
      sourceRef:
        kind: HelmRepository
        name: ocm
        namespace: flux-system
  values:
    clusterName: onprem-rancher-np
    registration:
      imagePullSpec: quay.io/open-cluster-management/registration:latest
    work:
      imagePullSpec: quay.io/open-cluster-management/work:latest